{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "tui",
  "globalEnv": [
    "JWT_SECRET",
    "JWT_REFRESH_SECRET",
    "ADMIN_EMAIL",
    "ADMIN_PASSWORD",
    "DEMO_EMAIL",
    "DEMO_PASSWORD",
    "NODE_ENV",
    "MONGO_URL",
    "PORT"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": []
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
